# Cursor master implementation prompt

Copy this into Cursor after opening the repository:

> Read AGENTS.md, all files in `.cursor/rules/`, README.md and the version-matched
> Next.js documentation bundled in `node_modules/next/dist/docs/`. Audit the
> CounterCrave repository without replacing the architecture. First run the
> existing validation, lint and build commands. Fix only reproducible problems.
> Preserve server-rendered content, draft-safe indexing, the
> `visitbest07-20` affiliate tag, accessible HTML and the no-scraping product
> image workflow. Do not invent ASINs, products, prices, ratings, tests or
> content. After fixes, report changed files, tests run and any remaining
> launch blockers.
